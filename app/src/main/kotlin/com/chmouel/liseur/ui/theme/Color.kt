package com.chmouel.liseur.ui.theme

import androidx.compose.ui.graphics.Color

// "Paper & ink" brand palette: white paper, dark ink, leather accents.
//
// Paper is white, as paper is. The warmth is in the ink and the leather,
// where it belongs: a page tinted the colour of a page is one of those
// things that looks charming for a second and grubby thereafter, and it
// makes every photograph on it -- which is to say every book cover --
// look as though the screen needs cleaning.
val Paper = Color(0xFFFFFFFF)

// Everything M3 stacks on top of the page: a menu, a sheet, a card, an
// app bar that has been scrolled under. The steps are small and warm --
// paper in shadow, not a grey card laid on a white desk. Cards fill
// most of a settings screen, and at any darker a tint they stop reading
// as part of the page and start reading as slabs sitting on it.
val PaperRaised = Color(0xFFFCFAF7)
val PaperCard = Color(0xFFF8F5F0)
val PaperHigh = Color(0xFFF4F0EA)
val PaperHighest = Color(0xFFF0EBE3)
val PaperDim = Color(0xFFE3DED6)
val PaperWarm = Color(0xFFEFE9E0)
val Rule = Color(0xFFD3CCC1)
val RuleStrong = Color(0xFF7E7466)
val Ink = Color(0xFF15110C)
val InkSoft = Color(0xFF4A3F30)
val InkInverse = Color(0xFF302B25)
val PaperInverse = Color(0xFFF6F1EA)
val Leather = Color(0xFF7A4A2B)
val LeatherLight = Color(0xFFFFDCC3)
val LeatherDark = Color(0xFF5C3018)
val Sage = Color(0xFF5B6650)
val SageLight = Color(0xFFDFE6D0)
val Teal = Color(0xFF3A5F5C)
val TealLight = Color(0xFFBDECE6)

val NightSurface = Color(0xFF17130E)
val NightSurfaceHigh = Color(0xFF241E16)
val NightText = Color(0xFFEBE1D0)
val LeatherNight = Color(0xFFE5A87C)
val SageNight = Color(0xFFBFC9AE)
val TealNight = Color(0xFFA2D3CD)
