<?xml version="1.0" encoding="utf-8"?>
<!-- Plain field behind the mark; the artwork carries its own ring, so this
     stays flat.  The mark keeps its cream field in both themes: inverted it
     read as grey hair. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="@color/logo_field"
        android:pathData="M0,0h108v108h-108z" />
</vector>
